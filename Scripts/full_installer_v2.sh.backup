#!/bin/bash

# ==============================================================================
# DOTFILES FULL INSTALLER v2.0 - JSON NATIVE
# Sistema completo unificado - Base de datos JSON única
# ==============================================================================

set -euo pipefail

# Configuraciones globales
readonly SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
readonly DOTFILES_DIR="$(dirname "$SCRIPT_DIR")"
readonly PACKAGES_JSON="$SCRIPT_DIR/packages.json"
readonly ADDITIONAL_DIR="$SCRIPT_DIR/Additional"
readonly LOG_DIR="$HOME/.local/share/dotfiles/logs"
readonly CONFIG_DIR="$HOME/.config/dotfiles"

# Crear directorios necesarios
mkdir -p "$LOG_DIR" "$CONFIG_DIR"

# Logging con timestamp
readonly LOG_FILE="$LOG_DIR/full_installation_$(date +%Y%m%d_%H%M%S).log"
exec 1> >(tee -a "$LOG_FILE")
exec 2> >(tee -a "$LOG_FILE" >&2)

# Contadores globales para resumen
TOTAL_INSTALLED=0
TOTAL_FAILED=0
TOTAL_SKIPPED=0

# ==============================================================================
# FUNCIONES DE UTILIDAD
# ==============================================================================

log() {
    local level="$1"
    shift
    echo "[$(date +'%Y-%m-%d %H:%M:%S')] [$level] $*" | tee -a "$LOG_FILE"
}

info() { log "INFO" "$@"; }
success() { log "SUCCESS" "\033[32m$*\033[0m"; }
warning() { log "WARNING" "\033[33m$*\033[0m"; }
error() { log "ERROR" "\033[31m$*\033[0m"; }

show_banner() {
    cat << 'EOF'
╔══════════════════════════════════════════════════════════════════════╗
║                   🚀 DOTFILES FULL INSTALLER v2.0                   ║
║                     Sistema Completo de Dotfiles                     ║
║                        Arch Linux + Hyprland                         ║
╚══════════════════════════════════════════════════════════════════════╝
EOF
}

show_section() {
    local section="$1"
    echo
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "🎯 $section"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
}

ask_yes_no() {
    local prompt="$1"
    local default="${2:-n}"
    local response
    
    while true; do
        read -p "$prompt [y/N]: " response
        response="${response:-$default}"
        case "${response,,}" in
            y|yes|s|si) return 0 ;;
            n|no) return 1 ;;
            *) echo "Por favor, responde con y/n (yes/no)" ;;
        esac
    done
}

check_system() {
    info "🔍 Verificando sistema..."
    
    # Verificar que estamos en Arch Linux
    if [[ ! -f /etc/arch-release ]]; then
        error "Este script está diseñado para Arch Linux"
        exit 1
    fi
    
    # Verificar conexión a internet
    if ! ping -c 1 8.8.8.8 >/dev/null 2>&1; then
        error "No hay conexión a internet"
        exit 1
    fi
    
    # Verificar que git está instalado
    if ! command -v git >/dev/null 2>&1; then
        info "Instalando git..."
        sudo pacman -S --needed --noconfirm git
    fi
    
    success "✅ Sistema verificado"
}

# ==============================================================================
# MÓDULOS DE INSTALACIÓN
# ==============================================================================

install_hyprland_custom() {
    show_section "Instalación de Hyprland Personalizado (end-4)"
    
    if ask_yes_no "¿Instalar configuración personalizada de Hyprland de end-4?"; then
        info "🌟 Instalando configuración de Hyprland..."
        
        # Crear backup de configuración existente si existe
        if [[ -d "$HOME/.config/hypr" ]]; then
            warning "Configuración de Hyprland existente detectada"
            if ask_yes_no "¿Crear backup de la configuración actual?"; then
                mv "$HOME/.config/hypr" "$HOME/.config/hypr.backup.$(date +%Y%m%d_%H%M%S)"
                info "✅ Backup creado"
            fi
        fi
        
        # Instalar configuración de end-4
        bash <(curl -s "https://end-4.github.io/dots-hyprland-wiki/setup.sh")
        
        success "✅ Configuración de Hyprland instalada"
    else
        info "⏭️  Instalación de Hyprland omitida"
    fi
}

install_packages_json() {
    show_section "Instalación de Paquetes (Sistema JSON v2.0)"
    
    if ask_yes_no "¿Ejecutar instalación de paquetes con el nuevo sistema JSON?"; then
        local package_installer="$SCRIPT_DIR/package_installer.sh"
        
        if [[ -f "$package_installer" ]]; then
            info "🚀 Ejecutando instalador de paquetes v2.0..."
            bash "$package_installer"
            success "✅ Instalación de paquetes completada"
        else
            warning "Instalador v2.0 no encontrado, usando sistema legacy..."
            install_packages_legacy
        fi
    else
        info "⏭️  Instalación de paquetes omitida"
    fi
}

install_packages_legacy() {
    show_section "Instalación de Paquetes (Sistema Legacy)"
    
    local legacy_installer="$SCRIPT_DIR/install-packages.sh"
    
    if [[ -f "$legacy_installer" ]]; then
        info "🔄 Ejecutando instalador legacy..."
        bash "$legacy_installer"
        success "✅ Instalación legacy completada"
    else
        error "❌ No se encontró ningún instalador de paquetes"
    fi
}

install_extra_packages() {
    show_section "Instalación de Paquetes Adicionales"
    
    if ask_yes_no "¿Instalar paquetes adicionales (TPM, NvChad, Starship, etc.)?"; then
        local extra_installer="$SCRIPT_DIR/install_extra_packs.sh"
        
        if [[ -f "$extra_installer" ]]; then
            info "📦 Ejecutando instalador de paquetes adicionales..."
            bash "$extra_installer"
            success "✅ Paquetes adicionales instalados"
        else
            warning "Script de paquetes adicionales no encontrado"
        fi
    else
        info "⏭️  Paquetes adicionales omitidos"
    fi
}

setup_dotfiles_links() {
    show_section "Configuración de Enlaces Simbólicos"
    
    if ask_yes_no "¿Configurar enlaces simbólicos para dotfiles?"; then
        local stow_script="$SCRIPT_DIR/stow-links.sh"
        
        if [[ -f "$stow_script" ]]; then
            info "🔗 Configurando enlaces simbólicos..."
            bash "$stow_script"
            success "✅ Enlaces simbólicos configurados"
        else
            warning "Script de stow no encontrado"
            
            # Fallback: configuración manual básica
            if command -v stow >/dev/null 2>&1; then
                info "📁 Configurando enlaces básicos con stow..."
                cd "$DOTFILES_DIR"
                
                local configs=("kitty" "nvim" "tmux" "zsh" "hyprland")
                for config in "${configs[@]}"; do
                    if [[ -d "Config/$config" ]]; then
                        info "🔗 Enlazando $config..."
                        stow -t "$HOME" "Config/$config" 2>/dev/null || true
                    fi
                done
                
                success "✅ Enlaces básicos configurados"
            else
                warning "stow no está disponible"
            fi
        fi
    else
        info "⏭️  Configuración de enlaces omitida"
    fi
}

run_additional_scripts() {
    show_section "Scripts Adicionales"
    
    if [[ ! -d "$ADDITIONAL_DIR" ]]; then
        warning "Directorio Additional no encontrado"
        return 0
    fi
    
    # Listar scripts disponibles
    local scripts=()
    while IFS= read -r -d '' script; do
        scripts+=("$script")
    done < <(find "$ADDITIONAL_DIR" -name "*.sh" -type f -print0)
    
    if [[ ${#scripts[@]} -eq 0 ]]; then
        info "No se encontraron scripts adicionales"
        return 0
    fi
    
    info "Scripts adicionales disponibles:"
    for i in "${!scripts[@]}"; do
        local script_name
        script_name=$(basename "${scripts[$i]}")
        echo "  $((i+1)). $script_name"
    done
    echo
    
    if ask_yes_no "¿Ejecutar scripts adicionales uno por uno?"; then
        for script in "${scripts[@]}"; do
            local script_name
            script_name=$(basename "$script")
            
            if ask_yes_no "¿Ejecutar $script_name?"; then
                info "🔧 Ejecutando $script_name..."
                
                # Hacer ejecutable si no lo es
                chmod +x "$script"
                
                # Ejecutar script
                if bash "$script"; then
                    success "✅ $script_name completado"
                else
                    warning "⚠️  $script_name terminó con errores"
                fi
            else
                info "⏭️  $script_name omitido"
            fi
        done
    else
        # Ofrecer ejecutar todos
        if ask_yes_no "¿Ejecutar todos los scripts adicionales?"; then
            for script in "${scripts[@]}"; do
                local script_name
                script_name=$(basename "$script")
                
                info "🔧 Ejecutando $script_name..."
                chmod +x "$script"
                
                if bash "$script"; then
                    success "✅ $script_name completado"
                else
                    warning "⚠️  $script_name terminó con errores"
                fi
            done
        else
            info "⏭️  Scripts adicionales omitidos"
        fi
    fi
}

optimize_system() {
    show_section "Optimización del Sistema"
    
    if ask_yes_no "¿Ejecutar optimizaciones del sistema?"; then
        info "🚀 Optimizando sistema..."
        
        # Limpiar cache de pacman
        info "🧹 Limpiando cache de pacman..."
        sudo pacman -Sc --noconfirm
        
        # Actualizar base de datos de archivos
        info "📄 Actualizando base de datos de archivos..."
        sudo updatedb 2>/dev/null || true
        
        # Actualizar cache de fuentes
        if command -v fc-cache >/dev/null 2>&1; then
            info "🔤 Actualizando cache de fuentes..."
            fc-cache -fv >/dev/null 2>&1
        fi
        
        # Generar configuración de GRUB si existe
        if [[ -f /boot/grub/grub.cfg ]] && command -v grub-mkconfig >/dev/null 2>&1; then
            if ask_yes_no "¿Regenerar configuración de GRUB?"; then
                info "🎛️  Regenerando GRUB..."
                sudo grub-mkconfig -o /boot/grub/grub.cfg
            fi
        fi
        
        success "✅ Optimización completada"
    else
        info "⏭️  Optimización omitida"
    fi
}

# ==============================================================================
# RESUMEN Y REPORTES
# ==============================================================================

show_installation_summary() {
    show_section "Resumen de Instalación"
    
    echo "📋 Información de la instalación:"
    echo "   📅 Fecha: $(date)"
    echo "   👤 Usuario: $USER"
    echo "   🖥️  Sistema: $(uname -a)"
    echo "   📂 Dotfiles: $DOTFILES_DIR"
    echo "   📄 Log: $LOG_FILE"
    echo
    
    # Verificar componentes instalados
    echo "🔍 Estado de componentes:"
    
    # Verificar Hyprland
    if command -v hyprctl >/dev/null 2>&1; then
        echo "   ✅ Hyprland instalado"
    else
        echo "   ❌ Hyprland no detectado"
    fi
    
    # Verificar algunos paquetes clave
    local key_packages=("kitty" "nvim" "tmux" "git" "stow")
    for pkg in "${key_packages[@]}"; do
        if command -v "$pkg" >/dev/null 2>&1; then
            echo "   ✅ $pkg disponible"
        else
            echo "   ❌ $pkg no disponible"
        fi
    done
    
    # Verificar configuraciones
    local configs=("$HOME/.config/kitty" "$HOME/.config/nvim" "$HOME/.config/tmux")
    for config in "${configs[@]}"; do
        local config_name
        config_name=$(basename "$config")
        if [[ -d "$config" ]]; then
            echo "   ✅ Configuración $config_name enlazada"
        else
            echo "   ❌ Configuración $config_name no enlazada"
        fi
    done
}

create_post_install_guide() {
    local guide_file="$HOME/dotfiles_post_install.md"
    
    cat > "$guide_file" << 'EOF'
# 🎉 Guía Post-Instalación de Dotfiles

## ✅ Instalación Completada

¡Felicidades! Tu sistema de dotfiles ha sido instalado exitosamente.

## 🔧 Próximos Pasos

### 1. Configuración de Terminal
- Reinicia tu terminal para aplicar la configuración de zsh
- Verifica que Starship esté funcionando
- Prueba los alias configurados

### 2. Configuración de Neovim
- Abre neovim: `nvim`
- Si instalaste NvChad, espera a que termine la configuración inicial
- Instala plugins con `:PackerSync` o `:Lazy sync`

### 3. Configuración de Tmux
- Inicia tmux: `tmux`
- Instala plugins con `Prefix + I` (Ctrl+B + I por defecto)

### 4. Configuración de Hyprland (si está instalado)
- Cierra sesión y selecciona Hyprland en el gestor de sesiones
- Familiarízate con los atajos de teclado configurados

## 🎨 Personalización

### Temas y Fuentes
- Las fuentes Nerd están instaladas
- Configura tu terminal para usar CaskaydiaCove Nerd Font

### Aplicaciones Recomendadas
- **Terminal**: Kitty (ya configurado)
- **Editor**: Neovim con NvChad
- **Navegador**: Chromium/Brave (instalados)
- **Reproductor**: MPV con MPD

## 📚 Documentación

### Comandos Útiles
```bash
# Gestión de paquetes
./json_manager.sh stats              # Ver estadísticas
./json_manager.sh validate           # Validar configuración
./package_installer.sh               # Reinstalar paquetes

# Dotfiles
cd ~/dotfiles
git pull                            # Actualizar dotfiles
stow Config/kitty                   # Reenlazar configuración
```

### Archivos de Configuración
- **Zsh**: `~/.config/zsh/.zshrc`
- **Kitty**: `~/.config/kitty/kitty.conf`
- **Neovim**: `~/.config/nvim/`
- **Tmux**: `~/.config/tmux/tmux.conf`

## 🐛 Solución de Problemas

Si encuentras problemas:
1. Revisa el log: `$LOG_FILE`
2. Valida el JSON: `./json_manager.sh validate`
3. Reinstala paquetes específicos: `./package_installer.sh`

## 📞 Soporte

- Documentación: Check README.md en el repositorio
- Issues: Abre un issue en GitHub si encuentras problemas

---
*Generado automáticamente por Dotfiles Full Installer v2.0*
EOF

    success "📖 Guía post-instalación creada en: $guide_file"
}

# ==============================================================================
# FUNCIÓN PRINCIPAL
# ==============================================================================

main() {
    show_banner
    
    info "🚀 Iniciando instalación completa de dotfiles..."
    echo
    
    # Verificaciones iniciales
    check_system
    
    # Asegurar permisos ejecutables
    info "🔒 Configurando permisos..."
    find "$SCRIPT_DIR" -name "*.sh" -type f -exec chmod +x {} \;
    
    # Menú principal de instalación
    info "📋 Módulos de instalación disponibles:"
    echo
    echo "Este instalador te guiará a través de varios módulos:"
    echo "  1. 🌟 Hyprland personalizado (end-4)"
    echo "  2. 📦 Paquetes del sistema (JSON v2.0)"
    echo "  3. 🔧 Paquetes adicionales (TPM, NvChad, etc.)"
    echo "  4. 🔗 Enlaces simbólicos de configuración"
    echo "  5. ⚙️  Scripts adicionales"
    echo "  6. 🚀 Optimizaciones del sistema"
    echo
    
    if ! ask_yes_no "¿Continuar con la instalación?"; then
        info "Instalación cancelada por el usuario"
        exit 0
    fi
    
    # Ejecutar módulos
    install_hyprland_custom
    install_packages_json
    install_extra_packages  
    setup_dotfiles_links
    run_additional_scripts
    optimize_system
    
    # Resumen final
    show_installation_summary
    create_post_install_guide
    
    echo
    success "🎉 ¡Instalación completa terminada exitosamente!"
    echo
    info "📖 Revisa la guía post-instalación en: ~/dotfiles_post_install.md"
    info "📄 Log completo disponible en: $LOG_FILE"
    echo
    warning "💡 Se recomienda reiniciar el sistema para aplicar todos los cambios"
    
    if ask_yes_no "¿Reiniciar el sistema ahora?"; then
        sudo reboot
    fi
}

# Manejar señales para limpieza
trap 'error "Instalación interrumpida"; exit 130' INT TERM

# Ejecutar si es llamado directamente
if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    main "$@"
fi
